/**
 * Represent a story status
 *
 * @author Dany Pignoux (dany.pignoux@outlook.fr)
 */
export class StoryStatus {
  /**
   * Constructor
   *
   * @param id The ID
   * @param key The key name
   */
  public constructor(
    public id?: number,
    public key?: string,
  ) {
  }
}
